# Stripe (server only — set in Vercel Project → Settings → Environment Variables)
STRIPE_SECRET_KEY=sk_test_...

# Optional: used for success/cancel redirect URLs when the browser does not send Origin
SITE_URL=https://your-domain.vercel.app
